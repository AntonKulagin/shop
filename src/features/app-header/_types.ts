type ImageType = {
  id: string;
  name: string;
  url: string;
};

export type LogoType = {
  id: string;
  title: string;
  image: ImageType;
};

export type PhoneType = {
  id: string;
  phone: string;
};
